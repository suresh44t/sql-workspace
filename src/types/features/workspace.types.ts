import { BaseComponentInterface } from '../ui.types'; 

// Main workspace component props
export interface WorkspaceInterface extends BaseComponentInterface { 
  // Currently only using base props 
}
